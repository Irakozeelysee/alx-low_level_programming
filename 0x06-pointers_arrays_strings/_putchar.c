#include "main.h"
#include <unistd.h>

/**
 * _putchar -Writes the character c to stdout.
 * @c: the character to print
 * Return: 1 sucess
 * -1 for an error.
 */

int _putchar(char c)
{
	return (write(1, &C, 1);
}
