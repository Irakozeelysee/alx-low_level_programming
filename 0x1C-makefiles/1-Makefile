COMPILER = gcc
SOURCE_FILES = main.c school.c
OBJECT_FILES = $(SOURCE_FILES:.c=.o)

all: school

school: $(OBJECT_FILES)
	$(COMPILER) -o school $^

%.o: %.c
	$(COMPILER) -c $< -o $@

clean:
	rm -f $(OBJECT_FILES) school
